{
  "frontend/src/index.css": "Global styles implementing the Yesfour Infra corporate theme with OKLCH color tokens for green primary, white backgrounds, and utility classes for all four task status colors (Red/Yellow/Blue/Green) in both solid and soft variants.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter font family, brand green colors, task status colors (task-red, task-yellow, task-blue, task-green with bg variants), card shadows, and all standard design tokens.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup defining routes for login, profile setup, and role-specific dashboards (employee/manager/admin), with a protected layout wrapper and Sonner toaster.",
  "frontend/src/hooks/useAuth.ts": "Custom auth hook wrapping useInternetIdentity and user profile query to expose isAdmin/isManager/isEmployee helpers, showProfileSetup flag, and logout function.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations including user profile CRUD, task operations with updated `useUploadProof` accepting `submittedByName`/`submittedByEmail`, new `useGetAllTasks` and `useGetAdminDashboard` hooks, and admin user management hooks — with `Principal` imported via ES module instead of `require()`.",
  "frontend/src/hooks/useCountdown.ts": "Hook that accepts a deadline in nanoseconds and returns a formatted countdown string, expired state, and urgency flag, updating every minute.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Yesfour Infra branding and Internet Identity sign-in button; fixes the TypeScript error by guarding against both `null` and `undefined` before accessing `userProfile.role`.",
  "frontend/src/pages/ProfileSetupPage.tsx": "First-time profile setup page collecting name, login email, department, and role; includes email format validation and passes all required `UserProfile` fields (including the new `email` field) to `saveCallerUserProfile`, then redirects to the role-appropriate dashboard.",
  "frontend/src/components/Layout.tsx": "Main app shell with sticky top navigation bar showing the Yesfour Infra logo/brand, role-based nav links, user info, logout button, and a branded footer with caffeine.ai attribution.",
  "frontend/src/components/StatusBadge.tsx": "Reusable colour-coded status badge component with task status badges plus new `ApprovalStatusBadge` (Pending Review/Approved/Rejected) and `ProofUploadedBadge` (blue) for admin-only proof visibility indicators.",
  "frontend/src/components/TaskCard.tsx": "Task card with role-based proof visibility; fixed `useCountdown` destructuring to use `formatted` (renamed to `countdown`) instead of the non-existent `countdown` property.",
  "frontend/src/components/ProofUploadButton.tsx": "File upload button accepting JPEG/PNG/PDF with upload progress tracking, now accepting `submittedByName` and `submittedByEmail` props and passing them to the `useUploadProof` mutation.",
  "frontend/src/components/RejectTaskModal.tsx": "Dialog modal for task rejection requiring a non-empty reason text, calling rejectTask mutation on submit with loading state and toast feedback.",
  "frontend/src/components/TaskApprovalActions.tsx": "Approve/Reject action buttons for Blue-status tasks; approve calls approveTask mutation with toast feedback, reject opens RejectTaskModal.",
  "frontend/src/components/TaskCreationForm.tsx": "Task creation form with all required fields; the \"Assign To\" field is now a searchable autocomplete combobox (`UserCombobox`) that fetches active users via `useGetActiveUsers`, displays options as \"Full Name (Email)\", filters in real time, stores the selected email for submission, and shows inline validation/backend errors. The `useCreateTask` mutation now passes `assigneeEmail` instead of a Principal.",
  "frontend/src/components/DepartmentProductivityPanel.tsx": "Panel showing task counts by status, completion rate, and total performance points; updated to use `TaskResponse` instead of the removed `Task` type, and extracts status keys via object key inspection.",
  "frontend/src/components/SummaryStatsCards.tsx": "Four stat cards showing Total Tasks, Completed, Overdue, and In Progress counts with colour-coded icons for the Admin Dashboard.",
  "frontend/src/components/LeaderboardTable.tsx": "Ranked leaderboard table displaying employee name, department, role, and performance points; now passes `entry.principal.toString()` to `useGetUserProfile` which expects a string.",
  "frontend/src/components/GlobalTaskList.tsx": "Filterable task grid with department/status/priority/search filters; now accepts `isAdminView` prop and passes it to TaskCard components so admin-only proof fields are shown only to Admin users.",
  "frontend/src/pages/EmployeeDashboard.tsx": "Employee dashboard passing `currentUserPrincipal` from identity to TaskCard components and `submittedByName`/`submittedByEmail` to ProofUploadButton, ensuring employees only see their own proof data.",
  "frontend/src/pages/ManagerDashboard.tsx": "Manager dashboard with tabbed interface for task creation, review/approval of Blue-status tasks, and department productivity analytics.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with summary stats, leaderboard, all-tasks (admin view), and users tabs; removed the non-existent `inProgressTasks` prop from `SummaryStatsCards` since it computes that value internally.",
  "frontend/src/components/UserAccountStatusBadge.tsx": "Reusable badge component displaying Active (green) or Inactive (grey) account status as a styled pill, consistent with the existing StatusBadge pattern and Yesfour Infra corporate theme.",
  "frontend/src/components/UsersSummaryCards.tsx": "Four analytics stat cards showing Total Users, Active Users, Total Managers, and Total Employees counts, styled consistently with SummaryStatsCards using the corporate green/white theme.",
  "frontend/src/components/EditUserRoleModal.tsx": "Dialog modal for editing a user's role; now calls `userPrincipal.toString()` before passing to `useUpdateUserRole` mutation which expects `user: string`.",
  "frontend/src/components/DeleteUserConfirmDialog.tsx": "AlertDialog confirmation for permanently deleting a user; now calls `userPrincipal.toString()` before passing to `useDeleteUser` mutation which expects a string.",
  "frontend/src/pages/UsersListPage.tsx": "Admin-only Users List page with summary analytics cards, search/filter controls (name/role/department), paginated table (10 per page) showing all user stats with role badges, status badges, and action buttons for edit/toggle/delete.",
  "frontend/src/components/ProofPreviewModal.tsx": "New modal component for Admin proof preview using shadcn Dialog, displaying image thumbnails or PDF file icons with metadata (submitted by, timestamp), and a download button using ExternalBlob.getDirectURL().",
  "frontend/src/components/ProofReviewPanel.tsx": "New Admin-only proof review panel showing employee name, submission timestamp, file thumbnail, View/Download buttons, and Approve/Reject action buttons with RejectTaskModal integration."
}