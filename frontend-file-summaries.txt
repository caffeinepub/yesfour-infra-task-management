{
  "frontend/src/index.css": "Global styles implementing the Yesfour Infra corporate theme with OKLCH color tokens for green primary, white backgrounds, and utility classes for all four task status colors (Red/Yellow/Blue/Green) in both solid and soft variants.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter font family, brand green colors, task status colors (task-red, task-yellow, task-blue, task-green with bg variants), card shadows, and all standard design tokens.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup defining routes for login, profile setup, and role-specific dashboards (employee/manager/admin), with a protected layout wrapper and Sonner toaster.",
  "frontend/src/hooks/useAuth.ts": "Custom auth hook wrapping useInternetIdentity and user profile query to expose isAdmin/isManager/isEmployee helpers, showProfileSetup flag, and logout function with safe role key extraction.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; fixes `useUploadProof` to call `actor.uploadProof(taskId, file)` with correct signature, adds `useMarkComplete` (calls `actor.markComplete`) and `useAdminReviewTask` (calls `actor.adminReviewTask` with decision and optional comment), all invalidating relevant query keys on success.",
  "frontend/src/hooks/useCountdown.ts": "Hook that accepts a deadline in nanoseconds and returns a formatted countdown string, expired state, and urgency flag, updating every minute.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Yesfour Infra branding and Internet Identity sign-in button; gets `login` and `isInitializing` directly from `useInternetIdentity` and uses `profileFetched` (renamed from `isFetched`) from `useAuth` for redirect logic.",
  "frontend/src/pages/ProfileSetupPage.tsx": "First-time profile setup page collecting name, login email, department, and role; includes email format validation and passes all required `UserProfile` fields (including the new `email` field) to `saveCallerUserProfile`, then redirects to the role-appropriate dashboard.",
  "frontend/src/components/Layout.tsx": "Main app shell with sticky top navigation bar showing the Yesfour Infra logo/brand, role-based nav links, user info, logout button, and a branded footer with caffeine.ai attribution.",
  "frontend/src/components/StatusBadge.tsx": "Reusable colour-coded status badge components for TaskStatus and ApprovalStatus enums, plus a ProofUploadedBadge, all using exhaustive enum key mappings from the backend types.",
  "frontend/src/components/TaskCard.tsx": "Task card with role-based proof visibility; updated to show `ProofUploadButton` for yellow/in-progress tasks (employee only), employee's own proof panel when task is blue/pending-review, admin proof review panel for admin view, and displays `reviewComment` from admin to employee after rejection.",
  "frontend/src/components/ProofUploadButton.tsx": "File upload button for JPEG/PNG/PDF with progress tracking; now accepts only `taskId` and `hasExistingProof` props, calls the corrected `useUploadProof` mutation, and includes a \"Mark as Completed\" button (enabled only after proof is uploaded) that calls `useMarkComplete`.",
  "frontend/src/components/RejectTaskModal.tsx": "Dialog modal for marking a task incomplete; updated label/copy to \"Mark Task as Incomplete\", requires a non-empty review comment, and passes the comment to the `onReject` callback.",
  "frontend/src/components/TaskApprovalActions.tsx": "Approve/Reject action buttons for Blue-status tasks using `useApproveTask` and `useRejectTask` mutations with toast feedback and RejectTaskModal integration.",
  "frontend/src/components/TaskCreationForm.tsx": "Task creation form with searchable autocomplete combobox for assignee selection, all required fields, and `useCreateTask` mutation passing `assigneeEmail`.",
  "frontend/src/components/DepartmentProductivityPanel.tsx": "Panel showing task counts by status, completion rate, and total performance points per department using `TaskResponse` type with safe enum key extraction.",
  "frontend/src/components/SummaryStatsCards.tsx": "Four stat cards showing Total Tasks, Completed, Overdue, and In Progress counts computed from `allTasks` array, using `TaskResponse` type from backend.",
  "frontend/src/components/LeaderboardTable.tsx": "Ranked leaderboard table displaying employee name, department, role, and performance points, passing `principal.toString()` to `useGetUserProfile`.",
  "frontend/src/components/GlobalTaskList.tsx": "Filterable task grid with department/status/priority/search filters, accepting `isAdminView` prop and passing it along with proof-related props to TaskCard components.",
  "frontend/src/pages/EmployeeDashboard.tsx": "Employee dashboard showing personal task stats and task cards; simplified to pass only `currentUserPrincipal` to TaskCard (removed `submittedByName`/`submittedByEmail` since the backend no longer requires them for proof upload).",
  "frontend/src/pages/ManagerDashboard.tsx": "Manager dashboard with tabbed interface for task creation, review/approval of Blue-status tasks, and department productivity analytics using `useGetAllTasks`.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with summary stats, leaderboard, all-tasks (admin view), and users tabs using `useGetAdminDashboard` and `useGetAllTasks` hooks with proper loading states.",
  "frontend/src/components/UserAccountStatusBadge.tsx": "Reusable badge component displaying Active or Inactive account status with safe enum key extraction supporting both string and object enum variants.",
  "frontend/src/components/UsersSummaryCards.tsx": "Four analytics stat cards showing Total Users, Active Users, Total Managers, and Total Employees counts with safe enum key extraction.",
  "frontend/src/components/EditUserRoleModal.tsx": "Dialog modal for editing a user's role, calling `userPrincipal.toString()` before passing to `useUpdateUserRole` mutation.",
  "frontend/src/components/DeleteUserConfirmDialog.tsx": "AlertDialog confirmation for permanently deleting a user, calling `userPrincipal.toString()` before passing to `useDeleteUser` mutation.",
  "frontend/src/pages/UsersListPage.tsx": "Admin-only Users List page with summary analytics cards, search/filter controls, paginated table showing all user stats with role badges, status badges, and action buttons for edit/toggle/delete.",
  "frontend/src/components/ProofPreviewModal.tsx": "Modal component for proof preview using shadcn Dialog, displaying image thumbnails or PDF file icons with metadata and a download button using ExternalBlob.getDirectURL().",
  "frontend/src/components/ProofReviewPanel.tsx": "Admin-only proof review panel showing employee name, submission/review timestamps, file preview, View/Download buttons, and Approve/\"Mark Incomplete\" actions; now uses `useAdminReviewTask` with `FinalStatus.approved`/`FinalStatus.rejected` and displays `reviewComment` after rejection."
}